const mocha = require("mocha")
const expect = require("chai").expect // Hämta expect
const {app} = require("../index")



describe("", () => {             //Rimlig rubrik 

    it("", ( )=>{                //namn som beskriver testet
        expect(app).to.exist    //chai syntax, för assert
        
    })
})